 pipenv run python manage.py dumpdata --natural-foreign --natural-primary --exclude=contenttypes --exclude=auth.permission --indent=2 > data_backup.json

 pipenv run python manage.py loaddata data_backup.json

 pipenv requirements > requirements.txt

 pipenv install psycopg2-binary  


This will install them and automatically add the pinned versions to your Pipfile and Pipfile.lock.
 pipenv install gunicorn 
 


Then regenerate your requirements.txt for Railway:
bashpipenv requirements > requirements.txt

Railway can actually use Pipfile directly, but having a requirements.txt is more reliable and widely supported.


start commmand in railway
~~~~~~~~~~~~~~~~~~~~~~~~~
Added this to the railway start command so it runs the static files 
python manage.py collectstatic --noinput && python manage.py migrate && gunicorn config.wsgi